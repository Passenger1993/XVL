XVL - X-Ray Vision Lab
Система для анализа сварных швов с использованием YOLO для детекции дефектов.

📋 Быстрый старт для тестирования
1. Установка зависимостей
bash
# Создание виртуального окружения (рекомендуется)
python -m venv .venv

# Активация виртуального окружения
# Для Windows:
.venv\Scripts\activate
# Для Linux/Mac:
source .venv/bin/activate

# Установка зависимостей проекта
pip install -e .

# Установка зависимостей для разработки и тестирования
pip install -e ".[dev]"

# Установка pre-commit хуков
pre-commit install

2. Запуск тестов
bash
# Запуск всех тестов
pytest

# Запуск всех тестов с подробным выводом
make test-all

# Запуск тестов генераторов изображений
make test-generators

# Тесты конкретных модулей
make test-crack      # Тесты генерации трещин
make test-pore       # Тесты генерации пор
make test-seam       # Тесты генерации швов
make test-fusion     # Тесты генерации непроваров
make test-empty      # Тесты пустых швов

# Запуск с покрытием кода
make coverage
# Открыть отчет в браузере:
open htmlcov/index.html  # Mac
start htmlcov/index.html # Windows

3. Проверка качества кода
bash
# Запуск всех проверок качества кода
make lint

# Автоматическое форматирование кода
make format

# Запуск pre-commit для всех файлов
pre-commit run --all-files

4. Основные команды Makefile
bash
# Полный список доступных команд
make help

# Основные команды:
make test           # Быстрый тест (без генераторов)
make test-all       # Все тесты
make coverage       # Тесты с отчетом о покрытии
make lint           # Проверка стиля кода
make format         # Форматирование кода
make clean          # Очистка временных файлов
make run            # Запуск приложения

5. Тестирование конкретных модулей
bash
# Тестирование генерации трещин
pytest tests/test_crack.py -v

# Тестирование генерации пор
pytest tests/test_pore.py -v

# Тестирование GUI (требуется X-server на Linux или GUI на Windows/Mac)
pytest tests/test_dialog.py -v -m "gui"

# Запуск тестов с маркировкой
pytest -m "integration"     # Только интеграционные тесты
pytest -m "not slow"        # Все кроме медленных тестов
🚨 Примечания для тестирования
Тесты генераторов изображений требуют библиотеки Pillow

GUI-тесты требуют наличия графической среды

Тесты с YOLO требуют установленной библиотеки ultralytics

Для быстрых тестов используйте make test (пропускает медленные тесты)

📊 Проверка результатов тестирования
После запуска тестов вы увидите:

✅ Количество пройденных тестов

❌ Количество упавших тестов (если есть)

📊 Отчет о покрытии кода (при использовании make coverage)

🔧 Устранение проблем
Если тесты не запускаются:

bash
# Проверьте установку зависимостей
pip list | grep pytest

# Проверьте конфигурацию pytest
pytest --version

# Запустите с подробным выводом для отладки
pytest -vv
📁 Структура проекта
text
tests/
├── test_crack.py              # Тесты генерации трещин
├── test_pore.py               # Тесты генерации пор
├── test_generate_seam.py      # Тесты генерации швов
├── test_incomplete_fusion.py  # Тесты генерации непроваров
├── test_empty.py              # Тесты пустых швов
├── test_predictor.py          # Тесты работы с моделью YOLO
├── test_dialog.py             # Тесты графического интерфейса
└── test_integration.py        # Интеграционные тесты